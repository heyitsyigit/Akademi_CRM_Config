# Entegrasyon ve kabul testi profili.
crm:
  environment: test

spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

management:
  endpoint:
    health:
      show-details: always
