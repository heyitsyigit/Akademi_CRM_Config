# Yerel geliştirme profili. Compose varsayılan olarak bu profili seçer.
crm:
  environment: dev

spring:
  jpa:
    properties:
      hibernate:
        format_sql: true

management:
  endpoint:
    health:
      show-details: always
